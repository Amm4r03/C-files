// writing code for bubblesort

#include <stdio.h>

void BubbleSort(int, int);

int main()
{
    printf("Enter the size of the array : ");
    int size;
    scanf("%d", &size);

    int array[size];

    printf("The array entered is as follows : ");

    return 0;
}
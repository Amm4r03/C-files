#include <stdio.h>
int main()
{
    int c = getchar();      // takes input for an int value of size 1
    putchar(c);             // prints the first digit of input number
    return 0;
}

/*c is declared as an integer. getchar awaits an input from the user and the putchar prints
out the recorded value */
// WAP to print ASCII number for a character

#include <stdio.h>
int main()
{
    printf("converts character input to its corresponding ASCII character\nEnter a character : ");
    char input;
    scanf("%c", &input);
    printf("%d", input);
    return 0;
}